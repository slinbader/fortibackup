#!/bin/sh
# prerm script for fortibackup
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Service stop/disable is handled by the systemd-helper #DEBHELPER#
        ;;

    failed-upgrade)
        ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

#DEBHELPER#

exit 0
